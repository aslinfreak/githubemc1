In my application,the dates are hardcoded,I dont need like this,actually there are done by the calendar in the application.Help me to fix this

Declare
 @AssociateId INT='2298944',
 @ComponentGroupID int='18',
 @Isactive int=0,
 @LoginID VARCHAR(10)='2298944',
 @CountryId int=15,
 @ChildType int=1

DECLARE @countrycode varchar(30),@Countryname varchar(30),@BusinessUnit varchar(30)
DECLARE @CurentDate DateTime = GETDATE()
SELECT @countrycode= Countryid , @Countryname = Country , @BusinessUnit=SetIdDivision
 from componentcountrymaster where PK_ComponentCountryID=@CountryId

 	DECLARE @ComponentcountrymappingID INT
	
	SELECT @ComponentcountrymappingID=CCM.PK_ComponentCountryMappingID 
	FROM COMPONENTCOUNTRYMAPPING CCM  WITH(NOLOCK) 
	INNER JOIN COMPONENTMASTER CM  WITH(NOLOCK) ON CM.PK_COMPONENTID=CCM.FK_COMPONENTID
	INNER JOIN ComponentGroupMaster CGM WITH (NOLOCK) ON CGM.PK_ComponentGroupID=CM.FK_ComponentGroupID 
	WHERE CGM.PK_ComponentGroupID=@COMPONENTGROUPID and CCM.FK_componentcountryid=@CountryID
ELSE IF (@ComponentGroupID = 18 and @CountryId <>2)
BEGIN
	IF EXISTS(SELECT Associate_ID FROM centralrepository.dbo.vw_centralrepository_associate_details with (nolock)
				WHERE associate_id=CAST(@AssociateId AS VARCHAR) AND IsActive IN ('A','L') AND Business_Unit LIKE('%'+@countrycode+'%') )
	BEGIN
		SELECT 1 AS STATUS,(ISNULL(LTRIM(RTRIM(AD.Associate_FirstName)),'')+' '+ISNULL(LTRIM(RTRIM(
		AD.Associate_LastName)),'')) AS AssociateName,
		ISNULL(DESG.JobCode+' - '+LTRIM(RTRIM(DESG.JobCodeDescription)),'') Level,CONVERT(VARCHAR,
		AD.DateOfJoining,101) AS DateOfJoining,AD.Business_Unit AssociateBU
		FROM CentralRepository.dbo.vw_CentralRepository_Associate_Details AD with (nolock)
			LEFT OUTER JOIN CENTRALREPOSITORY.dbo.vw_CentralRepository_Designation DESG WITH(NOLOCK) ON 
			DESG.JOBCODE=AD.JobCode AND DESG.Bussiness_unit=AD.Business_Unit 
		WHERE AD.Associate_ID=CAST(@AssociateId AS VARCHAR)

			
		IF exists(Select AssociateID from PR.CESchoolfees where AssociateId=CAST(@AssociateId AS VARCHAR) and 
		RowStatus=1 and ChildType=@ChildType 
		and (convert(date,@CurentDate) between StartDate and EndDate OR convert(date,@CurentDate) < StartDate))
		BEGIN
			Select top(1)1 as Status,StartDate,Enddate from PR.CESchoolfees where AssociateId=CAST(@AssociateId AS VARCHAR) and
			RowStatus=1 and ChildType=@ChildType 
			and (convert(date,@CurentDate) between StartDate and EndDate OR convert(date,@CurentDate) < StartDate) 
			order by createddate
		END
		ELSE
		BEGIN
			Select 0 as Status
		END					
	END
	ELSE IF EXISTS(SELECT Associate_ID FROM centralrepository.dbo.vw_centralrepository_associate_details with (nolock)
				WHERE associate_id=CAST(@AssociateId AS VARCHAR) AND IsActive IN ('A','L') AND Business_Unit not LIKE ('%'+@countrycode+'%'))
	BEGIN
	SELECT 0 AS STATUS,'Associate not belongs to '+@Countryname+'' AS ERRORMESSAGE
	END
	ELSE
	BEGIN
	SELECT 0 As Status,'Invalid AssociateID' AS ERRORMESSAGE
	END

END
