SELECT PRR.ProjectID, CP.*
FROM PR.Reversal PRR
LEFT JOIN CENTRALREPOSITORY.DBO.VW_CENTRALREPOSITORY_PROJECT CP
    ON CP.Project_ID = CONVERT(varchar(100), PRR.ProjectID)
WHERE PRR.AssociateID = '<AssociateID>';
